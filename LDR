int ldr = A0;
int led = 2;

void setup() {
Serial.begin(9600);
pinMode(led, OUTPUT);
}

void loop() {
int val = analogRead(ldr);

if (val < 300) {
Serial.println("Dark");

digitalWrite(led, HIGH);
delay(3000);
digitalWrite(led, LOW);
}
}

